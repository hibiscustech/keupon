<script Language="JavaScript">
<!--
function form_validator(){
var theForm = document.forms['open_deal_form'];
var i;
if (theForm.name.value == "" || theForm.name.value.length < 1)
{
alert("You must enter Deal Title.");
theForm.name.focus();
return (false);
}

if (theForm.actual_value.value == "" || theForm.actual_value.value.length < 1)
{
alert("You must enter Original Value.");
theForm.actual_value.focus();
return (false);
}

if (theForm.rules.value == "" || theForm.rules.value.length < 1)
{
alert("You must enter Rules.");
theForm.rules.focus();
return (false);
}

if (theForm.highlights.value == "" || theForm.highlights.value.length < 1)
{
alert("You must enter Highlights.");
theForm.highlights.focus();
return (false);
}

if (theForm.start_date.value == "" || theForm.start_date.value.length < 1)
{
alert("You must enter Start Date.");
theForm.start_date.focus();
return (false);
}

if (theForm.end_date.value == "" || theForm.end_date.value.length < 1)
{
alert("You must enter Close Date.");
theForm.end_date.focus();
return (false);
}


if (theForm.expiry_date.value == "" || theForm.expiry_date.value.length < 1)
{
alert("You must enter Expiry Date.");
theForm.expiry_date.focus();
return (false);
}

return true;
}


//-->
</script>

<br/>
<div style="width: 1000px; float:left;" id="summary">
  <%= render :partial => "open_deals_summary" %>
</div>
<br/>
<br/>
<div id="popup_create_deal">
    <%= render :partial => "create_edit_open_deal" %>
  </div>
  <div id="popup_view_deal">
    <%= render :partial => "open_deal_info" %>
  </div>

